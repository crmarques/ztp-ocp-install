---
- name: Skip provider BMC service for {{ bootwright_current_provider.name }}
  ansible.builtin.debug:
    msg: "Provider {{ bootwright_current_provider.name }} (kind {{ bootwright_current_provider.kind }}) has no provider BMC service."
  changed_when: false
