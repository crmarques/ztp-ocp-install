---
- name: Skip provider BMC service for {{ gitups_current_provider.name }}
  ansible.builtin.debug:
    msg: "Provider {{ gitups_current_provider.name }} (kind {{ gitups_current_provider.kind }}) has no provider BMC service."
  changed_when: false
