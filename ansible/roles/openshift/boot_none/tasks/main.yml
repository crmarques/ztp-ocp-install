---
- name: Skip BMC boot for provider {{ gitups_current_provider.name }}
  ansible.builtin.debug:
    msg: "Provider {{ gitups_current_provider.name }} (kind {{ gitups_current_provider.kind }}) has no BMC boot action."
  changed_when: false
