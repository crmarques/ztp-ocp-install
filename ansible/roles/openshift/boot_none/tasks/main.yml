---
- name: Skip BMC boot for provider {{ bootwright_current_provider.name }}
  ansible.builtin.debug:
    msg: "Provider {{ bootwright_current_provider.name }} (kind {{ bootwright_current_provider.kind }}) has no BMC boot action."
  changed_when: false
