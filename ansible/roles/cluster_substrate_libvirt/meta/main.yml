---
# host_libvirt installs libvirt + qemu packages on the orchestration host. The
# substrate role can't run without them, so the dependency travels with the
# role rather than via a playbook-level `when:` chain.
dependencies:
  - role: host_libvirt
