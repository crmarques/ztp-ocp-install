---
# Boot the hub VM via the vSphere API (PowerOnVM_Task with attached
# install-config ISO mounted as a CD-ROM). Scaffold only: implement
# alongside cluster_substrate_vsphere.

- name: vSphere hub boot is not yet implemented
  ansible.builtin.fail:
    msg: "hub_boot_vsphere is a scaffold; pair with cluster_substrate_vsphere when adding vSphere support."
