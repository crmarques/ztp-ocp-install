---
# Trusted collection dependencies. Keep this list exact-pinned: every entry
# must come from an official Ansible community or vendor namespace
# (community.*, ansible.*, containers.*, redhat.*).
collections:
  - name: community.general
    version: "12.6.0"
  - name: community.libvirt
    version: "2.2.0"
  - name: containers.podman
    version: "1.20.1"
  - name: ansible.posix
    version: "2.1.0"
  - name: community.crypto
    version: "3.2.0"
  - name: ansible.utils
    version: "6.0.2"
