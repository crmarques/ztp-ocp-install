---
- name: Publish managed-cluster intent to GitOps repo (placeholder)
  hosts: localhost
  gather_facts: false
  become: false
  tasks:
    - name: GitOps publish phase is not yet implemented
      ansible.builtin.debug:
        msg: |
          gitups phase=gitops-publish is a no-op placeholder. The CLI contract
          is wired, but rendering and pushing managed-cluster intent into the
          hub-watched Git repo is not yet implemented. See specs/state-model.md and
          specs/gitops.md for the planned contract.
