apiVersion: gitups.io/v1alpha1
kind: InfrastructureProvider
metadata:
  name: qemu-1-host-provider
spec:
  hosts:
    remote-qemu-host:
      ssh:
        address: 192.168.140.10
        user: root
        keyRef:
          name: remote-qemu-host-admin-key
      libvirtURI: qemu:///system
      capabilities:
        - libvirt
        - container-runtime
        - hosts-file
  machine:
    libvirt:
      hostRefs:
        - name: remote-qemu-host
      bmcEmulation:
        port: 8000
        auth:
          credentialRef:
            name: qemu-1-host-bmc-credentials
  loadBalancer:
    haProxy:
      hostRef:
        name: remote-qemu-host
  nameResolution:
    hostsFile:
      hostRefs:
        - name: remote-qemu-host
      additionalIngressHosts:
        - console-openshift-console.apps.qemu-1-host-hub.gitups.test
        - oauth-openshift.apps.qemu-1-host-hub.gitups.test
