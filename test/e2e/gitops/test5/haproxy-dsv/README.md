# haproxy-dsv

Declarest service-resources repository rendered by gitups from FullProvision "test5".

Reconciled by declarest against the `ManagedService/haproxy` CR declared in repo `controllers-dev`.

Contents are authored directly by users (or seeded by `declarest resource save`).
Each file's path must match the logical path layout declared by the bundle
the paired ManagedService's `spec.metadata.bundle` points at.
