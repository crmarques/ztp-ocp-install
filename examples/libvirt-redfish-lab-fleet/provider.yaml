apiVersion: bootwright.io/v1alpha1
kind: InfrastructureProvider
metadata:
  name: lab-hub-provider
spec:
  hosts:
    hub-sno-host:
      ssh:
        address: 10.10.10.1
        user: bootwright
        keyRef:
          name: lab-provider-key
      capabilities:
        - libvirt
        - hosts-file
  machine:
    libvirt:
      hostRefs:
        - name: hub-sno-host
      bmcEmulation:
        enabled: true
        protocol: redfish
        port: 8000
        auth:
          credentialRef:
            name: lab-bmc-credentials
  loadBalancer:
    haProxy:
      hostRef:
        name: hub-sno-host
  nameResolution:
    hostsFile:
      hostRefs:
        - name: hub-sno-host
---
apiVersion: bootwright.io/v1alpha1
kind: InfrastructureProvider
metadata:
  name: lab-managed-01-provider
spec:
  hosts:
    managed-01-host:
      ssh:
        address: 10.10.10.2
        user: bootwright
        keyRef:
          name: lab-provider-key
      capabilities:
        - libvirt
        - hosts-file
  machine:
    libvirt:
      hostRefs:
        - name: managed-01-host
      bmcEmulation:
        enabled: true
        protocol: redfish
        port: 8000
        auth:
          credentialRef:
            name: lab-bmc-credentials
  loadBalancer:
    haProxy:
      hostRef:
        name: managed-01-host
  nameResolution:
    hostsFile:
      hostRefs:
        - name: managed-01-host
---
apiVersion: bootwright.io/v1alpha1
kind: InfrastructureProvider
metadata:
  name: lab-managed-02-provider
spec:
  hosts:
    managed-02-host:
      ssh:
        address: 10.10.10.3
        user: bootwright
        keyRef:
          name: lab-provider-key
      capabilities:
        - libvirt
        - hosts-file
  machine:
    libvirt:
      hostRefs:
        - name: managed-02-host
      bmcEmulation:
        enabled: true
        protocol: redfish
        port: 8000
        auth:
          credentialRef:
            name: lab-bmc-credentials
  loadBalancer:
    haProxy:
      hostRef:
        name: managed-02-host
  nameResolution:
    hostsFile:
      hostRefs:
        - name: managed-02-host
